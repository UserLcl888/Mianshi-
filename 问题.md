# 问题记录

> 本文档用于记录项目开发过程中发现的问题：现象、原因、解决方案与状态。后续问题按"问题 N"追加，每个问题使用二级标题（非一级）。

**记录模板（新问题请按此结构追加）**

```markdown
## 问题 N：问题简述

**状态**：已修复 / 进行中 / 待处理（日期）

### 现象
（用户看到的异常表现，可附截图路径）

### 原因
（根因分析）

### 解决方案
（改了什么、改了哪些文件）

### 验证
（如何复测确认修复）
```

## 问题 1：管理员编辑题目时跳转到 404 页面

**状态**：已修复（2026-08-10）

### 现象

使用管理员账号（admin）登录后，在题目详情页点击右上角"编辑"按钮，页面跳转到 404（"页面不存在或已下架"），无法进入编辑页。

### 原因

前端路由配置（`user-web/src/router/index.ts`）中，管理页路由 `/admin` 与 `/admin/edit/:slug` 缺失（在之前的代码调整中被误删）。详情页"编辑"按钮跳转到 `/admin/edit/{slug}`，但没有对应路由匹配，最终落入兜底路由 `/:pathMatch(.*)*`，渲染出 404 页面。

### 解决方案

1. 恢复两条管理路由，并加权限标记：

```ts
{ path: '/admin', name: 'admin', component: () => import('@/views/AdminView.vue'),
  meta: { requiresAuth: true, requiresAdmin: true } },
{ path: '/admin/edit/:slug', name: 'admin-edit', component: () => import('@/views/AdminView.vue'),
  meta: { requiresAuth: true, requiresAdmin: true } }
```

2. 在路由守卫中补充管理员角色校验（非管理员访问管理页跳回首页）：

```ts
if (to.meta.requiresAdmin && auth.userInfo?.role !== 'ADMIN') {
  return { path: '/' }
}
```

### 验证

- 管理员登录 → 任意题目详情页点"编辑" → 正常进入编辑页并回填题目内容；
- 普通用户直接访问 `/admin` 或 `/admin/edit/{slug}` → 被守卫拦回首页。

## 问题 2：管理员编辑页体验优化（路径跳转、布局居中、正文 Markdown 预览）

**状态**：已修复（2026-08-10）

### 现象

- 管理员在题目详情页点击"编辑"后跳转路径异常（曾出现 404，见问题 1；页面未刷新时仍可能显示旧行为）；
- 编辑页表单整体偏左、不居中，观感不佳；
- 正文输入框是纯文本，无法预览 Markdown 效果（标题、列表、代码块、加粗等）。

### 原因

1. 管理路由 `/admin`、`/admin/edit/:slug` 曾在改动中丢失，导致编辑路径无路由匹配（问题 1 已修复）；
2. 编辑页 `.page-body` 未做居中处理，表单受限于窄宽度；
3. 编辑页没有 Markdown 渲染/预览能力，正文只能看原始文本。

### 解决方案

1. 恢复管理路由并补充管理员守卫（见问题 1 的解决方案）；
2. `AdminView.vue`：页面主体改为水平居中，内容区最大宽度 960px，表单占满卡片宽度；
3. `AdminView.vue`：新增轻量 Markdown 预览——正文区上方加"预览"按钮，弹出小窗口（宽 560px）实时渲染 `## 标题`、`- 列表`、```` ``` ```` 代码块、`**加粗**`、`[链接](url)`；渲染前先做 HTML 转义，避免 XSS。

### 验证

- 刷新页面后：管理员点"编辑" → 进入居中布局的编辑页；
- 在正文输入 `## 标题`、`- 列表`、```java 代码块、**加粗** → 点"预览" → 小窗口正确渲染，返回"保存修改"后详情页展示一致。

## 问题 3：编辑页面包屑分类不可点击、正文预览缺少标题层级

**状态**：已修复（2026-08-10）

### 现象

- 在"编辑面试题"页面，顶部面包屑中的分类（如 Redis，红框圈出）点击无反应；其他模块的分类同样不可点击；
- 点击"预览"后，正文没有渲染出层级与标题效果（如一级加黑标题），标题和正文混在一起显示。

### 原因

1. `AdminView.vue`（以及用户端 `ArticleDetailView.vue`）的面包屑对分类路径的最后一层做了特判：只有中间层级才带 `:to` 跳转，最后一层渲染为纯文本，导致顶层分类（如 Redis）不可点击；
2. 编辑页回填正文时用的是 `htmlToText(contentHtml)`，把已渲染的 HTML 转成纯文本，Markdown 标记（`## 标题`、`**加粗**` 等）全部丢失；后端详情接口也只返回 `contentHtml`，没有返回原始 `contentMd`，前端拿不到可重新渲染的 Markdown 源码。

### 解决方案

1. `user-web/src/views/AdminView.vue`、`user-web/src/views/ArticleDetailView.vue`：面包屑中分类项一律渲染为可点击链接（`/category/{slug}`），首页也指向 `/`；
2. `interview-backend`：`VOs.ArticleDetailVO` 增加 `contentMd` 字段，`ArticleService.toDetail()` 一并返回原始 Markdown；
3. `user-web/src/types/index.ts`：`ArticleDetail` 增加 `contentMd` 字段；
4. `user-web/src/views/AdminView.vue`：编辑页回填正文改为优先取 `a.contentMd`（为空时回退为原 `htmlToText` 逻辑），预览窗口即可按 Markdown 渲染标题层级、加粗等。

### 验证

- 管理员登录 → 进入任意题目编辑页 → 点击面包屑中的分类（如 Redis / Java / MySQL）→ 正常跳转到对应分类页；
- 编辑页点"预览" → `##` 标题、`**加粗**`、列表、代码块均按层级渲染，与详情页展示一致。

## 问题 4：分类下拉重复显示、缺少分类管理入口

**状态**：已修复（2026-08-10）

### 现象

- 编辑/新增题目页的"所属分类"下拉框中，没有子集的分类（Java基础、Java集合、JVM、JUC 等）会重复出现两次（分组标题 + 同名选项）；
- 需要新增主题或子主题时没有入口，只能在数据库或接口层面手动维护分类。

### 原因

1. `AdminView.vue` 中每个一级分类都无条件渲染 `el-option-group`，组内又放了一个同名选项；有子集的分类（如 MySQL）看起来是正常分组，叶子分类则明显重复；
2. 前端一直没有分类管理界面，后端 `/api/admin/categories` 的增删改接口早已存在但未接入前端。

### 解决方案

1. `AdminView.vue`：有子集的分类保留分组样式；无子集的分类直接渲染为单个选项，不再出现分组标题；
2. 新增 `user-web/src/components/admin/CategoryManageDialog.vue`：分类管理弹窗，一级主题行右侧提供"＋ 子集 / 编辑 / 删除"，列表底部单独一行"＋ 新主题"用于新增一级分类；新增/编辑时名称必填、slug 自动生成（`cat-时间戳`）可手动修改；
3. `AdminView.vue`：所属分类旁新增"管理分类"按钮打开弹窗；分类树变化后自动刷新下拉选项，若当前选中分类已被删除则清空选择；
4. `user-web/src/api/category.ts`：补齐 create/update/delete 三个管理接口封装。

### 验证

- 编辑页打开"所属分类"：叶子分类只显示一次，MySQL 等有子集的分类保持分组展示；
- 点"管理分类"→ 主题行"＋ 子集"可新增子集、"＋ 新主题"可新增一级分类、编辑/删除可用；
- 新增/编辑成功后下拉框即时刷新；删除有子集或题目的分类时后端报错并弹窗提示。

## 问题 5：登录方式改为邮箱/手机号 + 密码

**状态**：已修复（2026-08-10）

### 现象

原有登录仅支持用户名 + 密码；需求改为两种登录方式：邮箱 + 密码、手机号 + 密码，并保留 admin、demo 两个账号（登录标识改为 QQ 邮箱），同时支持后续按邮箱/手机号找回密码。

### 解决方案

1. 数据库（直接执行 SQL）：
   - `user` 表新增 `phone` 字段（可空、唯一索引 `uk_phone`）；
   - admin 账号邮箱改为 `2090323327@qq.com`，demo 账号邮箱改为 `2090323328@qq.com`，密码均为 123456；
2. 后端：
   - `Requests.LoginDTO` 的 `username` 改为 `account`（邮箱或手机号）；
   - `AuthService.login` 按 `email = account OR phone = account` 解析用户，限流 key 同步改为 account；
   - `AuthService.register` 支持邮箱或手机号注册（格式校验、唯一性校验），用户名可选、留空自动生成；
   - `User` 实体与 `UserVO` 增加 `phone` 字段；
3. 前端：
   - 登录页改为"邮箱登录 / 手机号登录"两个 Tab，对应格式校验；
   - 注册页改为"邮箱注册 / 手机号注册"两个 Tab；
   - 个人中心展示手机号；
   - 登录/注册接口与 store 改为传 `account`（邮箱或手机号）。

### 验证

- `2090323327@qq.com / 123456` 登录为管理员，`2090323328@qq.com / 123456` 登录为普通用户；
- 注册：新邮箱或新手机号可注册成功，重复注册被拦截；格式错误被拦截；
- 前端 `npm run build`、后端 `mvn compile` 均通过。
