# 后端设计文档（Spring Boot 3 + Sa-Token）

> 对应《思路.md》中的"后端（业务逻辑）"。职责：**提供 REST API、管理数据、控制权限**。本文档已按前端 `user-web/` 的联调契约更新。

---

## 1. 概述

| 项目 | 说明 |
|---|---|
| 技术栈 | Java 17 + Spring Boot 3 + MyBatis-Plus + Sa-Token + Redis + MySQL 8 |
| 职责 | 认证授权、内容 API、用户 API、管理 API、统计 API |
| 认证方案 | Sa-Token（token 会话存 Redis，支持踢人下线） |
| 接口分级 | 公开（注册/登录）/ 登录用户（`@SaCheckLogin`）/ 管理员（`@SaCheckRole("ADMIN")`） |

### 访问模型

- **未登录用户只能访问首页**：内容类接口（分类树、题目列表、详情）**全部要求登录**。
- 普通登录用户：只读内容、个人资料、改密码。
- 管理员：额外拥有内容新增/编辑/删除、用户管理、统计权限。

---

## 2. 项目结构

```text
interview-backend/
├─ pom.xml
└─ src/main/
   ├─ java/com/interview/
   │  ├─ InterviewApplication.java
   │  ├─ common/                 # Result / PageResult / BizException / ErrorCode / GlobalExceptionHandler
   │  ├─ config/                 # SaTokenConfig / WebConfig / RedisConfig / MybatisPlusConfig
   │  ├─ controller/
   │  │  ├─ AuthController.java
   │  │  ├─ UserController.java
   │  │  ├─ CategoryController.java
   │  │  ├─ ArticleController.java
   │  │  ├─ TagController.java
   │  │  └─ admin/
   │  │     ├─ AdminUserController.java
   │  │     ├─ AdminCategoryController.java
   │  │     ├─ AdminArticleController.java
   │  │     ├─ AdminTagController.java
   │  │     ├─ AdminStatsController.java
   │  │     └─ AdminLogController.java
   │  ├─ service/                # 接口 + impl
   │  ├─ mapper/                 # MyBatis-Plus Mapper + XML
   │  ├─ entity/                 # User/Category/Article/Tag/ArticleTag/AdminLog
   │  └─ dto/                    # 请求/响应 DTO
   └─ resources/
      ├─ application.yml
      ├─ db/init.sql             # 建表 + 种子账号（admin、demo）
      └─ mapper/*.xml
```

---

## 3. 数据模型（建表 SQL）

```sql
CREATE TABLE `user` (
  id            BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  username      VARCHAR(50)  NOT NULL UNIQUE COMMENT '用户名',
  password_hash VARCHAR(100) NOT NULL COMMENT 'BCrypt 哈希',
  nickname      VARCHAR(50)  NOT NULL DEFAULT '' COMMENT '昵称',
  avatar        VARCHAR(255) NOT NULL DEFAULT '' COMMENT '头像URL',
  email         VARCHAR(100) NOT NULL DEFAULT '' COMMENT '邮箱',
  role          VARCHAR(20)  NOT NULL DEFAULT 'USER' COMMENT 'USER/ADMIN',
  status        TINYINT      NOT NULL DEFAULT 1 COMMENT '1正常 0禁用',
  last_login_at DATETIME     NULL,
  created_at    DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at    DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  KEY idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='账号表';

CREATE TABLE `category` (
  id          BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  name        VARCHAR(50)  NOT NULL COMMENT '分类名',
  slug        VARCHAR(80)  NOT NULL UNIQUE COMMENT 'URL标识',
  parent_id   BIGINT UNSIGNED NOT NULL DEFAULT 0 COMMENT '0=顶级',
  sort_order  INT NOT NULL DEFAULT 0,
  description VARCHAR(255) NOT NULL DEFAULT '',
  created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  KEY idx_parent (parent_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='分类表（支持多级）';

CREATE TABLE `article` (
  id           BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  slug         VARCHAR(100) NOT NULL UNIQUE,
  title        VARCHAR(200) NOT NULL,
  summary      VARCHAR(500) NOT NULL DEFAULT '',
  category_id  BIGINT UNSIGNED NOT NULL,
  difficulty   VARCHAR(10)  NOT NULL DEFAULT '简单' COMMENT '简单/中等/困难',
  status       TINYINT NOT NULL DEFAULT 0 COMMENT '0草稿 1已发布',
  content_md   LONGTEXT NOT NULL COMMENT 'Markdown 原文',
  content_html LONGTEXT NOT NULL COMMENT '渲染消毒后的 HTML',
  view_count   BIGINT UNSIGNED NOT NULL DEFAULT 0,
  created_by   BIGINT UNSIGNED NULL COMMENT '创建管理员',
  created_at   DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at   DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  KEY idx_category (category_id, status),
  KEY idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='面试题表';

CREATE TABLE `tag` (
  id         BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  name       VARCHAR(50) NOT NULL UNIQUE,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='标签表';

CREATE TABLE `article_tag` (
  article_id BIGINT UNSIGNED NOT NULL,
  tag_id     BIGINT UNSIGNED NOT NULL,
  PRIMARY KEY (article_id, tag_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='题目-标签关联';

CREATE TABLE `admin_log` (
  id          BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  admin_id    BIGINT UNSIGNED NOT NULL,
  action      VARCHAR(50)  NOT NULL COMMENT '如 ARTICLE_CREATE',
  target_type VARCHAR(20)  NOT NULL COMMENT 'USER/CATEGORY/ARTICLE/TAG',
  target_id   BIGINT UNSIGNED NOT NULL,
  detail      VARCHAR(500) NOT NULL DEFAULT '',
  created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  KEY idx_admin (admin_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='管理员操作日志';
```

种子脚本创建两个账号（密码均为 BCrypt("123456")）：

| 账号 | 角色 | 用途 |
|---|---|---|
| `admin` | ADMIN | 管理员，内容管理 |
| `demo` | USER | 普通用户，只读浏览 |

---

## 4. API 设计

### 4.1 统一响应与错误码

```text
成功：{ "code": 200, "message": "ok", "data": ... }
分页：{ "code": 200, "message": "ok", "data": { "list": [...], "page": 1, "size": 10, "total": 123, "hasMore": true } }
```

| code | HTTP | 含义 |
|---|---:|---|
| 200 | 200 | 成功 |
| 40000 | 400 | 参数错误 |
| 40100 | **401** | 未登录 / token 失效（前端跳登录） |
| 40300 | 403 | 无权限 |
| 40400 | 404 | 资源不存在 |
| 40900 | 409 | 冲突（如用户名已存在） |
| 50000 | 500 | 服务器错误 |

> ⚠️ 401 必须同时返回 HTTP 401。

### 4.2 公开接口（无需登录）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/auth/register` | 注册 `{username, password, nickname?}` |
| POST | `/api/auth/login` | 登录 `{username, password}` → `{token, userInfo}` |

### 4.3 登录用户接口（`@SaCheckLogin`，未登录返回 401）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/categories/tree` | 分类树（含多级子分类） |
| GET | `/api/articles` | 题目列表 `?categoryId=&difficulty=&page=&size=` → `PageResult`（**顶级分类递归聚合子分类**） |
| GET | `/api/articles/{slug}` | 详情：`contentHtml` + `toc` + `categorySlug` + 相关题目 + 前后篇，浏览量 +1 |
| POST | `/api/auth/logout` | 退出 |
| GET | `/api/user/profile` | 当前用户信息 |
| PUT | `/api/user/password` | 修改密码 `{oldPassword, newPassword}` |

### 4.4 管理接口（`@SaCheckRole("ADMIN")`）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/admin/users` | 用户列表 `?keyword=&status=&page=&size=` |
| GET | `/api/admin/users/{id}` | 用户详情 |
| POST | `/api/admin/users` | 新增用户 |
| PUT | `/api/admin/users/{id}` | 修改昵称/邮箱/角色 |
| PUT | `/api/admin/users/{id}/status` | 禁用/启用（禁用时踢下线） |
| PUT | `/api/admin/users/{id}/password` | 重置密码 |
| DELETE | `/api/admin/users/{id}` | 删除用户 |
| GET/POST/PUT/DELETE | `/api/admin/categories[/{id}]` | 分类管理 |
| GET/POST | `/api/admin/articles` | 题目列表 / **新增题目** |
| PUT/DELETE | `/api/admin/articles/{id}` | **编辑题目** / 删除（含发布/下架） |
| POST | `/api/admin/articles/import` | Markdown 批量导入 |
| GET/POST/PUT/DELETE | `/api/admin/tags[/{id}]` | 标签管理 |
| GET | `/api/admin/stats/overview` | 统计：用户数/题目数/分类数/发布数 |
| GET | `/api/admin/stats/top-articles` | 浏览量 Top 10 |
| GET | `/api/admin/logs` | 操作日志分页 |

---

## 5. 认证与权限（Sa-Token）

```yaml
sa-token:
  token-name: Authorization   # 前端请求头名称
  timeout: 2592000            # 30 天
  is-concurrent: true
  is-share: false
  token-style: uuid
  is-log: false
```

```text
登录：校验密码（BCrypt）→ status=1 → StpUtil.login(userId) → 返回 token + userInfo
登出：StpUtil.logout()
禁用用户：status=0 → StpUtil.kickout(userId)
鉴权：内容/用户接口 @SaCheckLogin；管理接口 @SaCheckRole("ADMIN")
异常映射：NotLoginException → 40100（HTTP 401）；NotRoleException → 40300（HTTP 403）
```

登录限流：Redis 计数器 `rate:login:{username}:{minute}`，5 次/分钟。

---

## 6. 业务模块设计

### 6.1 认证模块

- 注册：用户名唯一 → BCrypt 加密 → 写入 user（role=USER, status=1）。
- 登录：校验密码 → 更新 last_login_at → 签发 token。

### 6.2 内容查询模块（需登录）

- **分类树**：全量查询内存组装树（按 sort_order 排序），Redis 缓存 30 分钟。
- **题目列表**：条件查询（categoryId、difficulty、status=1），分页返回摘要；`hasMore = page * size < total`。
  - ⚠️ **分类聚合**：传入顶级分类 `categoryId` 时，需递归包含其所有子分类的题目；传入子分类 id 时只返回该子分类题目。返回的题目 `categoryId` 必须是其真实所属分类（前端侧边栏据此分组）。
- **题目详情**：按 slug 查询 → 浏览量 Redis INCR → 返回 `contentHtml`、`toc`、`categoryName`、`categorySlug`、`related`、`prev/next`。
  - `categorySlug` 必须返回（前端面包屑/侧边栏依赖）；若题目属于子分类，则为子分类 slug。
- **相关题目**：同分类下其他已发布题取 3~5 条。
- **前后篇**：同分类按 id 排序取相邻两条（无则 null）。

### 6.3 用户模块

- 个人资料：从 `StpUtil.getLoginId()` 取当前用户。
- 修改密码：校验旧密码 → 新密码 BCrypt 更新 → 踢下线重新登录。

### 6.4 管理模块

- **用户管理**：列表（关键字/状态筛选）；禁用 → 踢下线；重置密码 → 返回随机密码（仅本次展示）。
- **题目新增/编辑**：接收 `{title, summary, categoryId, difficulty, tags[], contentMd}` → 渲染消毒 HTML → 入库/更新 → 清缓存。编辑保留原 slug。
- **批量导入**：解析 frontmatter（title/category/tags/difficulty/summary），逐条渲染入库，返回成功/失败明细。
- **操作日志**：AOP 切面（`@AdminLog("ARTICLE_UPDATE")`）写入 admin_log。

---

## 7. Markdown 渲染管道

```text
content_md
  → flexmark-java 渲染 HTML（GFM：表格/代码块/任务列表）
  → OWASP HTML Sanitizer 白名单消毒
  → 提取 h2/h3 生成 toc（锚点 id 与正文一致）
  → 存 content_html（保存/发布时生成一次）
```

白名单保留：`h1~h6、p、pre、code、table、thead、tbody、tr、th、td、img、a、ul、ol、li、blockquote、strong、em、br`，允许 `class="language-*"` 与锚点 `id`。

---

## 8. 缓存设计

| Key | 内容 | 失效策略 |
|---|---|---|
| `cache:category:tree` | 分类树 | 30 分钟 / 分类变更时删除 |
| `cache:article:detail:{slug}` | 详情 JSON | 内容更新时删除 |
| `view:article:{id}` | 浏览量计数 | 定时任务批量写库 |
| `rate:login:{username}:{minute}` | 登录限流 | 1 分钟 |
| Sa-Token 会话 | 登录 token | 过期 / 踢下线 |

---

## 9. 配置要点与部署

```yaml
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/interview?useUnicode=true&characterEncoding=utf8mb4
  data:
    redis:
      host: redis
server:
  port: 8080
```

- Dockerfile：`maven build → eclipse-temurin:17-jre`，暴露 8080。
- Docker Compose：`backend`、`mysql`、`redis`、`nginx`（托管前端 + 反代 `/api`）。
- 首次启动执行 `db/init.sql`（建表 + admin/demo 种子账号）。
