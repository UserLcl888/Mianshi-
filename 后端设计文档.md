# 后端设计文档（Spring Boot 3 + Sa-Token）

> 对应《思路.md》中的"后端（业务逻辑）"。职责：**提供 REST API、管理数据、控制权限**。本文档与 `interview-backend/` 当前实现保持一致。

---

## 1. 概述

| 项目 | 说明 |
|---|---|
| 技术栈 | Java 17 + Spring Boot 3 + MyBatis-Plus + Sa-Token + Redis + MySQL 8 |
| 职责 | 认证授权（邮箱/手机号登录、邮箱验证码、找回密码）、内容 API、用户 API、管理 API |
| 认证方案 | Sa-Token（token 会话存 Redis，支持踢人下线） |
| 接口分级 | 公开（注册/登录/验证码/分类树）/ 登录用户（拦截器校验）/ 管理员（`@SaCheckRole("ADMIN")`） |

### 访问模型

- **公开**：登录、注册、发验证码、验证码登录、找回密码、分类树（首页导航依赖）。
- **登录用户**：题目列表/详情、个人资料、修改昵称、修改密码。
- **管理员**：题目新增/编辑/删除、分类管理、用户管理、标签管理、统计、操作日志。

---

## 2. 项目结构

```text
interview-backend/
├─ pom.xml
└─ src/main/
   ├─ java/com/interview/
   │  ├─ InterviewApplication.java
   │  ├─ common/                 # Result / PageResult / BizException / ErrorCode / GlobalExceptionHandler
   │  ├─ config/                 # SaTokenConfig / MybatisPlusConfig / AppConfig
   │  ├─ controller/
   │  │  ├─ AuthController.java      # 登录/注册/验证码/找回密码/登出
   │  │  ├─ UserController.java      # 个人资料/改昵称/改密码
   │  │  ├─ CategoryController.java  # 分类树
   │  │  ├─ ArticleController.java   # 题目列表/详情/浏览上报
   │  │  └─ admin/
   │  │     ├─ AdminArticleController.java
   │  │     ├─ AdminCategoryController.java
   │  │     ├─ AdminUserController.java
   │  │     ├─ AdminTagController.java
   │  │     ├─ AdminStatsController.java
   │  │     └─ AdminLogController.java
   │  ├─ service/                # Auth / EmailCode / Article / Category / Markdown / Tag / Stats / AdminLog / AdminUser
   │  ├─ mapper/                 # MyBatis-Plus Mapper
   │  ├─ entity/                 # User / Category / Article / Tag / ArticleTag / AdminLog
   │  └─ dto/                    # Requests / VOs
   └─ resources/
      ├─ application.yml
      └─ application-example.yml
```

---

## 3. 数据模型（interview 库实际表结构）

```sql
CREATE TABLE `user` (
  `id`            bigint unsigned NOT NULL AUTO_INCREMENT,
  `password_hash` varchar(100) NOT NULL COMMENT 'BCrypt 哈希',
  `rootpassword`  varchar(50)  NOT NULL DEFAULT '' COMMENT '原始密码（演示/联调用）',
  `nickname`      varchar(50)  NOT NULL DEFAULT '' COMMENT '昵称',
  `avatar`        varchar(255) NOT NULL DEFAULT '' COMMENT '头像URL',
  `email`         varchar(100) NOT NULL DEFAULT '' COMMENT '邮箱',
  `phone`         varchar(20)  DEFAULT NULL COMMENT '手机号（可空，唯一）',
  `role`          varchar(20)  NOT NULL DEFAULT 'USER' COMMENT 'USER/ADMIN',
  `status`        tinyint      NOT NULL DEFAULT '1' COMMENT '1正常 0禁用',
  `last_login_at` datetime     DEFAULT NULL,
  `created_at`    datetime     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at`    datetime     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_phone` (`phone`),
  KEY `idx_status` (`status`),
  KEY `idx_role` (`role`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='账号表（管理员/普通用户）';

CREATE TABLE `category` (
  `id`          bigint unsigned NOT NULL AUTO_INCREMENT,
  `name`        varchar(50)  NOT NULL COMMENT '分类名',
  `slug`        varchar(80)  NOT NULL COMMENT 'URL标识',
  `parent_id`   bigint unsigned NOT NULL DEFAULT '0' COMMENT '0=顶级分类',
  `sort_order`  int NOT NULL DEFAULT '0',
  `description` varchar(255) NOT NULL DEFAULT '',
  `created_at`  datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at`  datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `slug` (`slug`),
  KEY `idx_parent` (`parent_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='分类表（多级）';

CREATE TABLE `article` (
  `id`           bigint unsigned NOT NULL AUTO_INCREMENT,
  `slug`         varchar(100) NOT NULL COMMENT 'URL标识',
  `title`        varchar(200) NOT NULL COMMENT '题目',
  `summary`      varchar(500) NOT NULL DEFAULT '',
  `doc_url`      varchar(500) NOT NULL DEFAULT '' COMMENT '文档链接',
  `category_id`  bigint unsigned NOT NULL,
  `difficulty`   varchar(10)  NOT NULL DEFAULT 'MEDIUM' COMMENT 'EASY/MEDIUM/HARD',
  `status`       tinyint NOT NULL DEFAULT '0' COMMENT '0草稿 1已发布',
  `content_md`   longtext NOT NULL COMMENT 'Markdown 原文',
  `content_html` longtext NOT NULL COMMENT '渲染并消毒后的 HTML',
  `view_count`   bigint unsigned NOT NULL DEFAULT '0',
  `created_by`   bigint unsigned DEFAULT NULL,
  `created_at`   datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at`   datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `slug` (`slug`),
  KEY `idx_category` (`category_id`,`status`),
  KEY `idx_status` (`status`),
  KEY `idx_difficulty` (`difficulty`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='面试题表';

CREATE TABLE `tag` (
  `id`         bigint unsigned NOT NULL AUTO_INCREMENT,
  `name`       varchar(50) NOT NULL,
  `created_at` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `name` (`name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='标签表';

CREATE TABLE `article_tag` (
  `article_id` bigint unsigned NOT NULL,
  `tag_id`     bigint unsigned NOT NULL,
  PRIMARY KEY (`article_id`,`tag_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='题目-标签关联';

CREATE TABLE `admin_log` (
  `id`          bigint unsigned NOT NULL AUTO_INCREMENT,
  `admin_id`    bigint unsigned NOT NULL,
  `action`      varchar(50)  NOT NULL,
  `target_type` varchar(20)  NOT NULL,
  `target_id`   bigint unsigned NOT NULL,
  `detail`      varchar(500) NOT NULL DEFAULT '',
  `created_at`  datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_admin` (`admin_id`),
  KEY `idx_target` (`target_type`,`target_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='管理员操作日志';
```

种子账号（密码均为 `123456`）：

| 登录标识 | 角色 | 用途 |
|---|---|---|
| `2090323327@qq.com` | ADMIN | 管理员，内容管理 |
| `2090323328@qq.com` | USER | 普通用户，只读浏览 |

---

## 4. 认证与登录方式

### 4.1 注册

- 邮箱或手机号至少填一项（格式校验 + 唯一性校验）。
- 昵称可选：不填时默认取登录账号（邮箱 @ 前部分 / 手机号）。
- 手机号未填时存 `NULL`（`uk_phone` 唯一索引允许多个 NULL，避免空串冲突）。
- 密码 BCrypt 加密，写入 `password_hash`，`rootpassword` 存明文（演示用）。

### 4.2 登录（邮箱或手机号 + 密码）

- `account` 同时匹配 `email` 或 `phone`。
- 账号不存在 → `40000 账号不存在`；密码错误 → `40000 密码错误`。
- 密码错误限流：Redis `rate:login:{account}`，1 分钟最多 5 次。
- 校验 `status=1` 后 `StpUtil.login(userId)` 签发 token，更新 `last_login_at`。

### 4.3 邮箱验证码登录

- `POST /api/auth/code/email`：校验邮箱已注册后生成 6 位验证码（`SecureRandom`），存 Redis 并发送邮件。
- `POST /api/auth/login/code`：校验验证码后签发 token。**邮箱必须已注册，不存在自动注册**。
- 开发环境 `app.code.mock=true` 时只打印日志并把验证码放入响应 `data.debugCode`，不发真实邮件。

### 4.4 邮箱找回密码

- `POST /api/auth/code/email`（`scene=reset`）：校验邮箱已注册后发码。
- `POST /api/auth/reset-password`：校验验证码 → BCrypt 更新密码 → `StpUtil.kickout(userId)` 踢下线。

### 4.5 验证码安全规则

- 一次性使用、5 分钟过期、错误 5 次自动作废。
- 60 秒重发冷却、单邮箱每日最多 10 次。
- 场景隔离：登录码与重置码使用不同 Redis key，互不可用。

---

## 5. API 设计

### 5.1 统一响应与错误码

```text
成功：{ "code": 200, "message": "ok", "data": ... }
分页：{ "code": 200, "message": "ok", "data": { "list": [...], "page": 1, "size": 10, "total": 123, "hasMore": true } }
```

| code | HTTP | 含义 |
|---|---:|---|
| 200 | 200 | 成功 |
| 40000 | 400 | 参数/业务错误 |
| 40100 | **401** | 未登录 / token 失效（前端跳登录） |
| 40300 | 403 | 无权限 |
| 40400 | 404 | 资源不存在 |
| 40900 | 409 | 冲突（邮箱/手机号已注册等） |
| 50000 | 500 | 服务器错误 |

> ⚠️ 401 必须同时返回 HTTP 401。

### 5.2 公开接口（Sa-Token 白名单）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/auth/register` | 注册 `{email?|phone?, password, nickname?}` |
| POST | `/api/auth/login` | 登录 `{account, password}` → `{token, userInfo}` |
| POST | `/api/auth/code/email` | 发邮箱验证码 `{email, scene: login|reset}` |
| POST | `/api/auth/login/code` | 邮箱验证码登录 `{email, code}` |
| POST | `/api/auth/reset-password` | 验证码重置密码 `{email, code, newPassword}` |
| GET | `/api/categories/tree` | 分类树（公开，首页导航依赖） |
| GET | `/api/home/overview` | 首页公开数据：文章数、总访问量、热门文档 Top5、热门标签 Top8 |

### 5.3 登录用户接口（拦截器校验，未登录返回 401）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/articles` | 题目列表 `?categoryId=&difficulty=&page=&size=`（顶级分类递归聚合子分类） |
| GET | `/api/articles/{slug}` | 详情：`contentHtml` + `contentMd` + `docUrl` + `toc` + `categorySlug` + `prev/next` |
| POST | `/api/articles/{id}/view` | 浏览上报（同用户 24h 去重） |
| POST | `/api/auth/logout` | 退出 |
| GET | `/api/user/profile` | 当前用户信息 |
| PUT | `/api/user/profile` | 修改昵称 `{nickname}` → 返回最新 UserInfo |
| PUT | `/api/user/password` | 修改密码 `{oldPassword, newPassword}`（成功踢下线） |

### 5.4 管理接口（`@SaCheckRole("ADMIN")`）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/admin/articles` | 管理端题目列表（含草稿，分类/难度筛选、分页，按 id 倒序） |
| POST / PUT / DELETE | `/api/admin/articles[/{id}]` | 新增/编辑/删除题目（payload 含 `docUrl?`，保存时渲染 HTML + 清分类缓存） |
| POST / PUT / DELETE | `/api/admin/categories[/{id}]` | 新增/编辑/删除分类（删除前校验子分类与题目） |
| GET | `/api/admin/users` | 用户列表 `?keyword=&status=&page=&size=` |
| GET / POST | `/api/admin/users[/{id}]` | 用户详情 / 新增用户 |
| PUT | `/api/admin/users/{id}` | 编辑用户（昵称/邮箱/角色） |
| PUT | `/api/admin/users/{id}/status` | 禁用/启用（禁用踢下线） |
| PUT | `/api/admin/users/{id}/password` | 重置密码 |
| DELETE | `/api/admin/users/{id}` | 删除用户 |
| GET / POST / PUT / DELETE | `/api/admin/tags[/{id}]` | 标签列表/新增/重命名/删除 |
| GET | `/api/admin/stats/overview` | 统计：用户数/题目数/分类数/发布数 |
| GET | `/api/admin/stats/top-articles` | 浏览量 Top 10 |
| GET | `/api/admin/stats/category-stats` | 各顶级分类（聚合子分类）的题目数与浏览量 |
| GET | `/api/admin/logs` | 操作日志分页 |

> 管理操作（用户/分类/题目/标签）都会写入 `admin_log`。

---

## 6. Sa-Token 配置

```yaml
sa-token:
  token-name: Authorization   # 前端请求头名称
  timeout: 2592000            # 30 天
  is-concurrent: true
  is-share: false
  token-style: uuid
  is-log: false
```

拦截器：`/api/**` 全部要求登录，白名单见 5.2；异常映射 `NotLoginException → 401`、`NotRoleException → 403`。

---

## 7. 业务模块设计

### 7.1 认证与验证码

- `EmailCodeService`：验证码生成（SecureRandom 6 位）、Redis 存取、限流、邮件发送（JavaMailSender / mock）。
- `AuthService`：注册/登录/验证码登录/找回密码/改昵称/改密码。
- 邮件发送：`spring-boot-starter-mail`，QQ SMTP 465 + SSL，发件人 = `spring.mail.username`。

### 7.2 内容查询

- **分类树**：全量查询内存组装（sort_order 升序），Redis 缓存 30 分钟，分类变更时删除。
- **题目列表**：条件查询（categoryId、difficulty、status=1）分页；`hasMore = page * size < total`；顶级分类递归聚合子分类（`collectIds`）。
- **题目详情**：按 slug 查询，返回 `contentHtml`、`contentMd`（编辑页回填用）、`toc`、`categorySlug`、`prev/next`。
- **浏览上报**：Redis `view:user:{userId}:article:{articleId}` 24h 去重后 `view_count + 1`。

### 7.3 用户模块

- 个人资料从 `StpUtil.getLoginId()` 获取。
- 修改昵称：校验非空后更新，返回最新 UserInfo。
- 修改密码：校验旧密码 → 更新 BCrypt → 踢下线重新登录。

### 7.4 管理模块

- **题目新增/编辑**：`{title, slug?, summary, categoryId, difficulty, tags[], contentMd}` → 渲染消毒 HTML → 入库/更新（编辑保留原 slug）→ 替换标签。
- **分类管理**：新增支持 `parentId` 挂子集；删除时校验存在子分类或题目则拒绝。
- **操作日志**：管理接口手动调用 `AdminLogService.write(...)` 写入。

---

## 8. Markdown 渲染管道

```text
content_md
  → flexmark（GFM：表格/代码块等）渲染 HTML
  → OWASP HTML Sanitizer 白名单消毒
  → 为 h1~h6 生成锚点 id（slugify + 去重）
  → 存 content_html；toc 由后端从 content_html 提取
```

白名单保留：`h1~h6、p、pre、code、table、thead、tbody、tr、th、td、img、a、ul、ol、li、blockquote、strong、em、br、hr`，允许 `class="language-*"` 与锚点 `id`。

---

## 9. Redis Key 设计

| Key | 内容 | TTL |
|---|---|---|
| `cache:category:tree` | 分类树 JSON | 30 分钟 / 变更时删除 |
| `view:user:{uid}:article:{aid}` | 浏览去重 | 24 小时 |
| `rate:login:{account}` | 登录失败次数 | 1 分钟 |
| `code:email:{email}:{scene}` | 验证码 | 5 分钟 |
| `code:email:{email}:{scene}:cooldown` | 重发冷却 | 60 秒 |
| `code:email:{email}:{scene}:tries` | 验证错误次数 | 5 分钟 |
| `rate:code:email:{email}:day` | 单邮箱每日发码数 | 24 小时 |

---

## 10. 配置要点（application.yml）

- MySQL：`interview` 库，账号 root/rootlcl，`map-underscore-to-camel-case`。
- Redis：127.0.0.1:6379，密码 java。
- 邮件：`spring.mail.host/port/username/password`（QQ 465 + SSL + 授权码），`app.code.mock` 控制模拟/真实发信。
- 验证码参数：`app.code.ttl-minutes=5`、`max-tries=5`、`resend-seconds=60`、`daily-limit=10`。
