# 管理后台设计文档（Vue 3 + Element Plus）

> 对应《思路.md》中的"管理后台（账号管理为核心）"。职责：**账号管理为主，内容管理为辅**，只给管理员使用。

---

## 1. 概述

| 项目 | 说明 |
|---|---|
| 定位 | 管理员专属后台：账号管理（核心）+ 内容管理 + 统计 |
| 技术栈 | Vue 3 + TypeScript + Vite + Vue Router + Pinia + Element Plus + Axios |
| 认证 | Sa-Token token（与用户端同后端，接口路径 `/api/admin/**`） |
| 权限 | 仅 `role=ADMIN` 可访问；后端二次校验兜底 |

---

## 2. 目录结构

```text
admin-web/
├─ src/
│  ├─ api/
│  │  ├─ request.ts            # axios 实例（与用户端同约定）
│  │  ├─ auth.ts               # 管理员登录/登出
│  │  ├─ user.ts               # 账号管理接口
│  │  ├─ category.ts           # 分类管理接口
│  │  ├─ article.ts            # 题目管理/导入接口
│  │  ├─ tag.ts
│  │  ├─ stats.ts
│  │  └─ log.ts
│  ├─ components/
│  │  ├─ MarkdownEditor.vue    # Markdown 编辑 + 实时预览（核心组件）
│  │  ├─ DataTable.vue         # el-table 封装（loading/分页/空态）
│  │  └─ ConfirmDialog.vue     # 二次确认弹窗封装
│  ├─ layout/
│  │  └─ AdminLayout.vue       # 侧边栏 + 顶栏 + 面包屑
│  ├─ router/index.ts          # 路由 + 管理员守卫
│  ├─ stores/adminAuth.ts
│  ├─ directives/permission.ts # v-permission 按钮级权限
│  └─ views/
│     ├─ login/LoginView.vue
│     ├─ dashboard/DashboardView.vue
│     ├─ user/UserListView.vue
│     ├─ user/UserDetailView.vue
│     ├─ category/CategoryManageView.vue
│     ├─ article/ArticleListView.vue
│     ├─ article/ArticleEditView.vue
│     ├─ article/ArticleImportView.vue
│     ├─ tag/TagManageView.vue
│     └─ log/LogListView.vue
├─ .env.development / .env.production
└─ package.json
```

---

## 3. 路由与布局

| 路径 | 页面 | 说明 |
|---|---|---|
| `/admin/login` | 登录页 | 独立布局（无侧边栏） |
| `/admin` | AdminLayout（守卫：已登录 + role=ADMIN） | 重定向到 dashboard |
| `/admin/dashboard` | 数据统计 | — |
| `/admin/users` | 用户列表 | 账号管理核心页 |
| `/admin/users/:id` | 用户详情 | — |
| `/admin/categories` | 分类管理 | — |
| `/admin/articles` | 题目列表 | — |
| `/admin/articles/create` | 新建题目 | — |
| `/admin/articles/:id/edit` | 编辑题目 | — |
| `/admin/articles/import` | 批量导入 | — |
| `/admin/tags` | 标签管理 | — |
| `/admin/logs` | 操作日志 | — |

### AdminLayout

```text
┌──────────┬──────────────────────────────┐
│ 侧边栏菜单 │ 顶栏：面包屑 + 管理员信息/退出    │
│ Dashboard│                              │
│ 账号管理  │     页面内容区（el-main）        │
│ 内容管理  │                              │
│ 操作日志  │                              │
└──────────┴──────────────────────────────┘
```

侧边栏菜单：数据统计 / 账号管理 / 内容管理（分类、题目、标签）/ 操作日志。

---

## 4. 登录与权限

### 登录流程

```text
/admin/login 输入账号密码
  → POST /api/auth/login（账号须为 ADMIN）
  → 存 token 到 localStorage
  → 跳转 /admin/dashboard
```

### 路由守卫

```text
进入 /admin/**（除 login）：
  无 token 或角色非 ADMIN → 跳 /admin/login
  token 过期（401）→ 清 token → 跳登录
```

### 按钮级权限

- 当前只有一种管理角色，`v-permission` 指令预留；后续角色细分时按权限码控制按钮显隐。
- **后端始终兜底**：前端隐藏只是体验优化，接口层 `@SaCheckRole("ADMIN")` 才是真正防线。

---

## 5. 账号管理模块（核心）

### 5.1 用户列表页（UserListView）

**筛选区**：关键字输入（用户名/昵称）、状态下拉（全部/正常/禁用）。

**表格列**：

| 列 | 说明 |
|---|---|
| ID | — |
| 用户名 | 唯一 |
| 昵称 / 邮箱 | — |
| 角色 | 标签展示（USER/ADMIN） |
| 状态 | 正常（绿色）/ 禁用（红色）Tag |
| 注册时间 / 最后登录 | 格式化时间 |
| 操作 | 详情 / 编辑 / 禁用·启用 / 重置密码 / 删除 |

**分页**：每页 10/20/50 条可切换。

### 5.2 用户详情页（UserDetailView）

- 基本信息只读展示（用户名、角色、状态、创建时间、最后登录）。
- 可编辑：昵称、邮箱、角色（下拉）。
- 账号操作：禁用/启用、重置密码、删除（与列表页同一套弹窗）。

### 5.3 关键操作流程

**禁用/启用**

```text
点击"禁用" → 二次确认弹窗（提示：该用户将被立即踢下线，无法登录）
  → PUT /api/admin/users/{id}/status {status: 0}
  → 成功刷新列表
```

**重置密码**

```text
点击"重置密码" → 弹窗输入新密码（6~32 位，二次确认）
  → PUT /api/admin/users/{id}/password
  → 成功提示"密码已重置"（该用户旧会话被踢下线，需重新登录）
```

**角色分配**

```text
编辑弹窗中切换 USER/ADMIN → 保存
  → 提示：升级为管理员需谨慎；降级后其管理员会话踢下线
```

**删除**

```text
点击"删除" → 二次确认（提示：建议改用禁用）
  → DELETE /api/admin/users/{id}
```

---

## 6. 内容管理模块

### 6.1 分类管理（CategoryManageView）

- 树形表格展示多级分类，列：名称、slug、排序、题目数、操作。
- 操作：新增子分类、编辑、上移/下移、删除。
- 删除校验：存在子分类或题目时后端返回 40000，前端弹窗展示后端 message。

### 6.2 题目管理

**列表页（ArticleListView）**

- 筛选：分类（级联下拉）、难度、状态（草稿/已发布）。
- 表格列：标题、分类、难度、标签、浏览量、状态、更新时间、操作（编辑/发布·下架/删除）。

**编辑页（ArticleEditView，核心页面）**

```text
┌───────────────────────────────────────────────┐
│ 基本信息表单（标题/分类/难度/标签/摘要/slug）        │
├───────────────────────┬───────────────────────┤
│ MarkdownEditor（左）   │ 实时预览（右，同渲染效果） │
└───────────────────────┴───────────────────────┘
底部按钮：保存草稿 / 发布 / 返回
```

- 保存/发布时后端重新渲染 HTML 并清缓存。
- 标签输入：可多选已有标签，也可输入新标签（后端自动创建）。

**批量导入（ArticleImportView）**

- 支持拖拽上传 `.md` 文件或直接粘贴 Markdown 文本。
- 上传后展示导入结果：成功 N 条、失败 M 条、失败原因列表（如"分类不存在"）。
- 导入前可勾选"直接发布"或"先存草稿"。

### 6.3 标签管理（TagManageView）

- 表格：ID、标签名、关联题目数、创建时间。
- 操作：重命名、合并（把 A 合并到 B，题目自动迁移）、删除（解除关联）。

---

## 7. 数据统计模块（DashboardView）

- 顶部统计卡片：用户总数、题目总数、分类数、已发布数。
- 浏览量 Top 10 表格：标题、分类、浏览量。
- 数据来源：`GET /api/admin/stats/overview`、`GET /api/admin/stats/top-articles`（后端缓存 10 分钟）。

---

## 8. 操作日志模块（LogListView）

- 表格列：时间、操作人、动作（如"禁用用户"）、目标类型、目标 ID、详情。
- 关键字筛选 + 分页；数据来源 `GET /api/admin/logs`。

---

## 9. API 对接与请求封装

与用户端共用同一套约定：

```text
baseURL: /api
请求头：Authorization: <token>
响应 code === 200 → 返回 data
响应 code === 401 → 清 token → 跳 /admin/login
其他非 200 → ElMessage.error(message)
```

所有管理接口集中在 `src/api/*.ts`，页面不直接写请求逻辑。

---

## 10. 组件设计

| 组件 | 职责 | 说明 |
|---|---|---|
| MarkdownEditor | 编辑 + 实时预览 | 预览样式与用户端一致，保证所见即所得 |
| DataTable | 表格封装 | 统一 loading、分页、空态 |
| ConfirmDialog | 二次确认弹窗 | 危险操作统一入口（禁用/删除/重置密码） |

---

## 11. 交互规范

- **所有危险操作（禁用/删除/重置密码）必须二次确认**，并明确提示后果。
- 禁用用户时提示"将被踢下线"；重置密码后提示"旧会话失效"。
- 表单提交中按钮 loading，防止重复提交。
- 导入失败必须逐条展示失败原因，而不是笼统报错。
- 列表操作成功后刷新当前页数据；编辑类操作成功后提示并返回列表。

---

## 12. 与用户端、后端的边界

- 管理后台**只调用 `/api/auth/**` 与 `/api/admin/**` 接口**，不出现用户端内容接口以外的访问。
- 所有写操作的最终校验在后端；前端权限控制仅用于体验。
