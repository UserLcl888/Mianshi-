# 管理后台设计文档（Vue 3 + Element Plus，集成于 user-web）

> 对应《思路.md》中的"管理后台"。**当前实现将管理员功能集成在用户端 `user-web/` 内**，未单独拆分 admin-web 工程；管理接口全部走 `/api/admin/**`，后端以 `@SaCheckRole("ADMIN")` 兜底。

---

## 1. 概述

| 项目 | 说明 |
|---|---|
| 定位 | 管理员在站内直接管理：统计、题目、分类、用户、标签、操作日志 |
| 入口 | 顶栏用户下拉“管理后台” → `/admin/dashboard`；“添加内容” → `/admin`；详情页“编辑” → `/admin/edit/:slug` |
| 权限 | 路由守卫要求登录 + role=ADMIN；后端接口二次校验 |
| 认证 | 与用户端同一套 Sa-Token token |

**已实现范围**：数据统计、题目管理（列表/删除）、添加/编辑题目、Markdown 批量导入、分类管理、用户管理、标签管理、操作日志。

---

## 2. 路由与入口

| 路径 | 页面 | 说明 |
|---|---|---|
| `/admin` | AdminView | 添加面试题 |
| `/admin/edit/:slug` | AdminView | 编辑面试题（自动回填原始 Markdown） |
| `/admin/dashboard` | AdminLayout + DashboardView | 数据统计 |
| `/admin/articles` | AdminLayout + ArticleManageView | 题目管理（列表/删除） |
| `/admin/categories` | AdminLayout + CategoryManageView | 分类管理（页面版） |
| `/admin/users` | AdminLayout + UserManageView | 用户管理 |
| `/admin/tags` | AdminLayout + TagManageView | 标签管理 |
| `/admin/logs` | AdminLayout + LogListView | 操作日志 |

管理台整体结构：`AdminLayout`（左侧菜单 + 顶栏面包屑/返回前台/退出）+ 各页面通过 `<router-view>` 挂载。

路由守卫：`meta.requiresAdmin` 且非 ADMIN → 跳回首页；未登录 → 跳登录。

---

## 3. 题目管理（AdminView）

### 3.1 表单字段

| 字段 | 说明 |
|---|---|
| 标题 | 必填 |
| 固定链接 slug | 可选，留空后端自动生成 |
| 摘要 | 一句话概括 |
| 所属分类 | 必填；有子集的分类分组展示，叶子分类单条展示；“管理分类”按钮打开分类管理弹窗 |
| 难度 | 简单 EASY / 中等 MEDIUM / 困难 HARD |
| 标签 | 逗号分隔，保存时后端自动创建/关联 |
| 正文 | Markdown 原文；顶部“预览”按钮弹出窗口实时渲染 |

### 3.2 Markdown 预览

- 前端 `marked` 渲染 + `DOMPurify` 消毒，样式与详情页一致（h1/h2 层级、表格、代码块高亮）。
- 编辑时回填 `contentMd`（详情接口已返回原始 Markdown），保证预览与保存一致。

### 3.3 保存流程

```text
校验标题/分类 → POST /api/admin/articles 或 PUT /api/admin/articles/{id}
  → 后端渲染消毒 HTML、替换标签、写 admin_log
  → 前端跳转 /article/{slug}
```

- 编辑保存保留原 slug；未保存修改离开页面时弹确认（`utils/unsaved.ts`）。

### 3.4 Markdown 批量导入

- 编辑/添加页“正文”工具栏右侧新增“导入 Markdown”按钮，只负责**正文内容**：选择一个 `.md` 文件后，自动去掉文件头 frontmatter，把正文填入正文输入框（替换当前内容）。
- 导入不涉及分类、难度、标签——这些字段仍在表单中手动填写；导入后可点“预览”查看渲染效果，再保存。
- 纯前端逻辑（FileReader 读取），不调用后端导入接口。

---

## 4. 分类管理（CategoryManageDialog）

从题目表单“管理分类”按钮打开，树形列表：

| 操作 | 说明 |
|---|---|
| 主题行右侧“＋ 子集” | 在该主题下新增子分类（输入名称 + slug，slug 自动生成可改） |
| 底部“＋ 新主题” | 新起一行新增一级分类 |
| 编辑 | 修改名称 / slug |
| 删除 | 二次确认；其下有子分类或题目时后端拒绝并提示 |

操作成功 → `POST/PUT/DELETE /api/admin/categories[/{id}]` → 刷新分类树 → 题目表单下拉同步。

---

## 5. 接口依赖

| 接口 | 用途 |
|---|---|
| `GET /api/articles/{slug}` | 编辑页回填（含 contentMd） |
| `POST /api/admin/articles` | 新增题目 |
| `PUT /api/admin/articles/{id}` | 编辑题目 |
| `POST/PUT/DELETE /api/admin/categories[/{id}]` | 分类增删改 |

---

## 6. 管理台页面清单

| 页面 | 功能 |
|---|---|
| DashboardView | 统计卡片（用户/题目/分类/已发布）+ 左侧浏览量 Top10 + 右侧两个 ECharts 饼图（分类浏览量、分类题目分布，按顶级分类聚合子分类） |
| ArticleManageView | 分类/难度筛选、分页列表、编辑、删除（页面标题“题目管理”） |
| AdminView | 添加/编辑题目、Markdown 预览、批量导入、分类管理弹窗 |
| CategoryManageView | 分类树：新增子集/新主题、编辑、删除 |
| UserManageView | 用户列表（关键字/状态筛选）、新增、编辑、禁用/启用、重置密码、删除 |
| TagManageView | 标签列表、新增、重命名、删除 |
| LogListView | 操作日志分页列表 |

所有管理写操作均写入 `admin_log`；危险操作（删除/禁用/重置密码）都有二次确认。

---

## 7. 权限与安全

- 前端路由守卫仅为体验优化，真正的权限控制在后端 `@SaCheckRole("ADMIN")`。
- 所有管理写操作写入 `admin_log`（动作/对象/详情）。
- 删除分类/用户/标签前，后端校验关联数据，避免破坏内容。
