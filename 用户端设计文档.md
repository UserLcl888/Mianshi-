# 用户端设计文档（Vue 3 SPA）

> 对应《思路.md》中的"用户端（前端展示）"。职责：**只做展示与交互，不做业务逻辑**，所有数据通过 REST API 从后端获取。本文档与 `user-web/` 当前实现保持一致。

---

## 1. 概述

| 项目 | 说明 |
|---|---|
| 定位 | PC 端面试题知识库展示网站 |
| 形态 | 单页应用（SPA），前后端分离 |
| 平台 | 现代桌面浏览器，按 ≥1280px 设计，不做移动端适配 |
| 技术栈 | Vue 3 + TypeScript + Vite + Vue Router + Pinia + Element Plus + Axios + highlight.js |
| 主题 | 淡黄色系（米白背景 + 琥珀色主色，CSS 变量统一管理） |
| 当前不含 | 站内搜索、积分/会员、AI 问答、语音 |

### 访问模型（重要）

- **未登录用户只能访问首页**（以及登录/注册页），不能看到分类与题目内容。
- 登录用户（普通用户 / 管理员）可浏览全部内容。
- 管理员额外拥有"添加 / 编辑内容"权限。

### Mock 账号（开发环境）

| 账号 | 密码 | 角色 |
|---|---|---|
| `admin` | `123456` | ADMIN（可添加/编辑内容） |
| `demo` | `123456` | USER（只读浏览） |

注册的新账号也会加入 mock 用户池并可登录（刷新页面后重置）。

---

## 2. 项目目录结构

```text
user-web/
├─ public/
│  └─ favicon.svg
├─ src/
│  ├─ api/                     # 接口层（唯一允许发请求的模块）
│  │  ├─ request.ts            # axios 实例 + 拦截器（Authorization / 401 处理）
│  │  ├─ auth.ts               # 注册/登录/退出/个人资料/改密码
│  │  ├─ category.ts           # 分类树（含 useMock 开关）
│  │  └─ article.ts            # 题目列表/详情
│  ├─ mock/                    # 前端 mock 数据与接口（VITE_USE_MOCK=true 时生效）
│  │  ├─ data.ts               # 分类树、题目、账号、增改函数
│  │  └─ index.ts              # mock API 实现（登录校验、分类聚合等）
│  ├─ components/
│  │  ├─ layout/
│  │  │  ├─ AppHeader.vue      # 顶栏（Logo/居中导航/登录或用户下拉）
│  │  │  ├─ CategorySidebar.vue# 左侧栏（分类树/子分类分组折叠/题目列表）
│  │  │  └─ AppFooter.vue
│  │  ├─ article/
│  │  │  ├─ ArticleCard.vue    # 列表卡片
│  │  │  ├─ DifficultyTag.vue  # 难度徽标
│  │  │  ├─ TocPanel.vue       # 详情页目录
│  │  │  ├─ RelatedArticles.vue# 相关题目（右侧独立栏）
│  │  │  └─ PrevNextNav.vue    # 上一篇/下一篇
│  │  └─ common/
│  │     └─ EmptyState.vue
│  ├─ router/index.ts          # 路由 + 权限守卫
│  ├─ stores/
│  │  ├─ auth.ts               # 登录态（token/userInfo/角色）
│  │  └─ category.ts           # 分类树缓存
│  ├─ views/
│  │  ├─ HomeView.vue          # 首页（公开）
│  │  ├─ CategoryView.vue      # 分类/子分类题目列表（无限滚动）
│  │  ├─ ArticleDetailView.vue # 题目详情
│  │  ├─ LoginView.vue
│  │  ├─ RegisterView.vue
│  │  ├─ AdminView.vue         # 内容管理（添加/编辑，仅管理员）
│  │  ├─ NotFoundView.vue      # 404
│  │  └─ profile/
│  │     ├─ ProfileView.vue    # 个人信息
│  │     └─ ChangePasswordView.vue
│  ├─ types/                   # TS 类型定义（Article/Category/User/PageResult）
│  ├─ styles/theme.css         # 淡黄色主题 + 布局变量
│  ├─ App.vue
│  └─ main.ts
├─ .env.development            # VITE_USE_MOCK=true
├─ .env.production             # VITE_USE_MOCK=false
├─ vite.config.ts              # /api 代理 → http://localhost:8080
└─ package.json
```

---

## 3. 路由与权限守卫

| 路径 | 页面 | 访问控制 |
|---|---|---|
| `/` | HomeView 首页 | **公开**（未登录唯一可看的内容页） |
| `/category/:slug` | CategoryView 分类/子分类列表 | **需登录** |
| `/article/:slug` | ArticleDetailView 题目详情 | **需登录** |
| `/login` | LoginView 登录 | 仅未登录可访问 |
| `/register` | RegisterView 注册 | 仅未登录可访问 |
| `/profile` | ProfileView 个人信息 | 需登录 |
| `/profile/password` | ChangePasswordView 修改密码 | 需登录 |
| `/admin` | AdminView 添加内容 | 需登录 + 角色 ADMIN |
| `/admin/edit/:slug` | AdminView 编辑内容 | 需登录 + 角色 ADMIN |
| `/:pathMatch(.*)*` | NotFoundView 404 | 公开 |

### 路由守卫逻辑（`router.beforeEach`）

```text
to.meta.requiresAuth = true 且未登录      → 跳转 /login?redirect=原路径（登录后回跳）
to.meta.requiresAdmin = true 且非 ADMIN  → 跳转 /
to 是 /login 或 /register 且已登录        → 跳转 /
其余放行
```

---

## 4. 页面设计

### 4.1 首页（HomeView，公开）

- 顶部栏 + 左侧栏（显示全部顶级分类，不显示题目）+ 欢迎区 + 分类入口卡片。
- 未登录时欢迎语为"登录后即可查看全部面试题内容"，点击分类卡片会跳登录。
- 已登录时欢迎语为常规介绍，点击分类卡片进入对应分类页。

### 4.2 分类 / 子分类列表页（CategoryView，需登录）

- 左侧栏：标题 = 当前分类名 + 题目总数。
  - 分类有子分类（如 MySQL）→ 按子分类分组显示，**默认折叠**，点箭头展开/收起，点子分类名进入该子分类页。
  - 分类无子分类 → 直接平铺该分类题目。
- 内容区：
  - 面包屑按层级显示（首页 / MySQL / 索引）。
  - 难度筛选下拉（全部/简单/中等/困难）。
  - **无限滚动**：每页 10 条，触底加载下一页，加载中显示"加载中…"，全部加载完显示"已经到底啦"。
- 子分类页（如 `/category/mysql-index`）：内容区只列该子分类题目，面包屑保留上级路径，顶部导航仍高亮顶级分类（MySQL）。

### 4.3 题目详情页（ArticleDetailView，需登录）

- 三栏布局：左侧 TOC 目录 | 中间正文 | **右侧独立栏：相关题目**（吸顶，利用右侧空白）。
- 正文：后端渲染消毒后的 `contentHtml`，前端 `v-html` + highlight.js 高亮。
- 面包屑按分类层级显示：首页 / MySQL / 日志 / 题目名。
- 底部：上一篇/下一篇。
- 管理员登录时，标题栏显示"编辑"按钮 → `/admin/edit/:slug`。

### 4.4 登录 / 注册页

- 登录：严格校验账号密码（mock 仅 `admin/123456`、`demo/123456` 与注册账号可登录），错误提示"账号或密码错误"。
- 登录页底部展示测试账号提示。
- 注册：用户名 3~20 位、密码 6~32 位、两次密码一致；注册成功后跳登录页。

### 4.5 个人中心与修改密码（需登录）

- 个人信息：用户名、昵称、邮箱、注册时间（只读展示）。
- 修改密码：旧密码 + 新密码 + 确认，成功后清登录态重新登录。

### 4.6 内容管理页（AdminView，仅管理员）

- `/admin`：添加面试题；`/admin/edit/:slug`：编辑已有题目（自动回填）。
- 表单字段：标题、摘要、所属分类（支持子分类，分组下拉）、难度、标签（逗号分隔）、正文（可选，留空用默认模板）。
- 保存后跳转该题详情页；编辑保存保留原 URL（slug 不变）、更新时间刷新。

---

## 5. 组件设计

| 组件 | 职责 | 关键输入/事件 |
|---|---|---|
| AppHeader | 顶栏：Logo（左）、导航（居中）、登录按钮或用户下拉 | `isLoggedIn`、`isAdmin`；下拉项：个人中心/修改密码/添加内容(ADMIN)/退出 |
| CategorySidebar | 左侧栏：分类树 / 子分类分组（默认折叠）/ 题目列表 | `activeCategorySlug`、`activeArticleSlug` |
| ArticleCard | 列表题目卡片 | `article: ArticleListItem` |
| DifficultyTag | 难度徽标（简单绿/中等黄/困难红） | `difficulty` |
| TocPanel | 详情页目录 + 滚动高亮 | `toc`、`activeId` |
| RelatedArticles | 右侧独立栏相关题目 | `articles: ArticleListItem[]` |
| PrevNextNav | 上一篇/下一篇 | `prev / next` |
| EmptyState | 空数据占位 | `text` |

---

## 6. 状态管理（Pinia）

### auth store

```text
state:  token（localStorage）、userInfo（含 role）、loading
getters: isLoggedIn
actions:
  login(username, password)   → POST /api/auth/login → 存 token + userInfo
  register(form)              → POST /api/auth/register
  logout()                    → POST /api/auth/logout → 清本地
  fetchProfile()              → GET /api/user/profile
  clearAuth()                 → 清 token/userInfo（401 或改密后调用）
```

### category store

```text
state: tree、loaded
actions: fetchTree()          → GET /api/categories/tree（缓存）
```

---

## 7. API 层设计

### request.ts（axios 实例）

```text
baseURL: /api
请求拦截器：存在 token 时设置 Authorization: <token>
响应拦截器：
  HTTP 401 → 清登录态 → 跳 /login?redirect=当前路径
  HTTP 403 → 提示无权限
  code !== 200 → ElMessage 提示后端 message
```

### 接口模块

| 模块 | 方法 | 说明 |
|---|---|---|
| auth.ts | `register` / `login` / `logout` / `getProfile` / `changePassword` | 认证与个人 |
| category.ts | `getCategoryTree` | 分类树（含子分类） |
| article.ts | `getArticles(params)` / `getArticleDetail(slug)` | 列表（分页 + hasMore）/ 详情 |

> mock 模式（`VITE_USE_MOCK=true`）下以上接口由 `src/mock/` 实现，不经过 axios。

---

## 8. 关键交互细节

### 无限滚动（分类列表页）

```text
初始化：page=1 → GET /api/articles 加载第一页，追加渲染
滚动触底（rootMargin 120px）→ hasMore 且未加载中 → page+1 加载下一页
全部加载完（hasMore=false）→ 显示"已经到底啦"
切换分类/修改筛选 → 重置 page=1、清空列表、重新加载
```

### 子分类折叠（侧边栏）

```text
进入有子分类的顶级分类（如 MySQL）：
  → 分组渲染各子分类，默认全部收起
  → 点右侧箭头：展开/收起该组题目（箭头旋转 180°）
  → 点子分类名：进入子分类页 /category/{subSlug}
  → 当前正在查看的题目所属分组自动展开
```

### 详情页加载

```text
GET /api/articles/{slug}
  → 渲染 contentHtml（已消毒，直接 v-html）
  → highlight.js 高亮代码块
  → 目录滚动监听高亮当前章节
  → 右侧渲染相关题目、底部渲染上一篇/下一篇
```

### 登录回跳与权限

- 游客访问受保护页面 → `/login?redirect=原路径`，登录成功后回跳。
- 管理员详情页显示"编辑"按钮；普通用户无此入口（后端兜底校验）。

---

## 9. 样式与布局

### 主题（theme.css）

```text
背景 #fffaf0 / 卡片 #fffdf6 / 边框 #f0e1b9 / 主色琥珀 #d9a716
Element Plus 主色覆盖为琥珀色系（--el-color-primary 及 light/dark 梯度）
```

### 布局骨架（PC 优先）

```text
┌────────────────────────────────────────────────┐
│ 顶栏（全宽）：Logo(左) │ 导航(居中) │ 登录(右)      │
├──────────┬──────────────────────┬──────────────┤
│ 左侧栏 250px│ 内容区（max 1160px）  │ 相关题目 250px │
│ （靠左偏移，│                      │ （详情页专属）  │
│  吸顶）    │                      │               │
└──────────┴──────────────────────┴──────────────┘
```

布局由 CSS 变量控制（`--brand-width`、`--header-gap`、`--layout-pad-x`、`--sidebar-offset` 等），后续调整间距只改变量。

---

## 10. 环境与构建

```text
.env.development:  VITE_USE_MOCK=true   （开发环境走 mock，无需后端）
.env.production:   VITE_USE_MOCK=false  （联调/生产走真实 API）

npm run dev      # 本地开发（http://localhost:5173）
npm run build    # 产物 dist/，Nginx 托管
```

---

## 11. 与后端、管理后台的边界

- 用户端只调用公开认证接口 + 登录用户内容接口 + 管理员内容管理接口。
- 所有权限最终由后端兜底（Sa-Token 注解鉴权），前端路由守卫仅作体验优化。
- 未登录禁止访问内容接口（后端同样需 `@SaCheckLogin`）。
