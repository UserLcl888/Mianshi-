# 用户端设计文档（Vue 3 SPA）

> 对应《思路.md》中的"用户端（前端展示）"。职责：**只做展示与交互，不做业务逻辑**，所有数据通过 REST API 从后端获取。本文档与 `user-web/` 当前实现保持一致。

---

## 1. 概述

| 项目 | 说明 |
|---|---|
| 定位 | PC 端面试题知识库展示网站 |
| 形态 | 单页应用（SPA），前后端分离，**无 mock，直接调真实后端** |
| 平台 | 现代桌面浏览器，按 ≥1280px 设计 |
| 技术栈 | Vue 3 + TypeScript + Vite + Vue Router + Pinia + Element Plus + Axios + highlight.js + marked + DOMPurify |
| 主题 | 淡黄色系（米白背景 + 琥珀色主色，CSS 变量统一管理） |
| 当前不含 | 站内搜索、短信验证码、积分/会员、AI 问答 |

### 访问模型

- **公开**：首页、登录/注册/找回密码、分类树（顶栏导航展示分类名）。
- **需登录**：分类页、题目详情、个人中心、修改密码。
- **管理员**：内容管理（添加/编辑题目）、分类管理。

### 测试账号

| 登录标识 | 密码 | 角色 |
|---|---|---|
| `2090323327@qq.com` | `123456` | ADMIN（可添加/编辑内容） |
| `2090323328@qq.com` | `123456` | USER（只读浏览） |

---

## 2. 项目目录结构

```text
user-web/
├─ public/
│  └─ favicon.svg
├─ src/
│  ├─ api/                     # 接口层（唯一允许发请求的模块）
│  │  ├─ request.ts            # axios 实例 + 拦截器（Authorization / 401 处理）
│  │  ├─ auth.ts               # 登录/注册/验证码/找回密码/个人资料/改昵称/改密码
│  │  ├─ category.ts           # 分类树 + 分类管理接口
│  │  └─ article.ts            # 题目列表/详情/新增/编辑
│  ├─ components/
│  │  ├─ layout/
│  │  │  ├─ AppHeader.vue      # 顶栏（Logo/居中导航/登录或用户下拉）
│  │  │  ├─ CategorySidebar.vue# 左侧栏（分类树/子分类分组折叠/题目列表）
│  │  │  └─ AppFooter.vue
│  │  ├─ article/
│  │  │  ├─ ArticleCard.vue    # 列表卡片
│  │  │  ├─ DifficultyTag.vue  # 难度徽标
│  │  │  ├─ TocPanel.vue       # 详情页目录（滚动高亮）
│  │  │  └─ PrevNextNav.vue    # 上一篇/下一篇
│  │  ├─ common/
│  │  │  ├─ EmptyState.vue
│  │  │  └─ VerifyCodeButton.vue  # 验证码发送按钮（60s 倒计时）
│  │  └─ admin/
│  │     └─ CategoryManageDialog.vue  # 分类管理弹窗
│  ├─ router/index.ts          # 路由 + 权限守卫
│  ├─ stores/
│  │  ├─ auth.ts               # 登录态（token/userInfo/角色）
│  │  └─ category.ts           # 分类树缓存
│  ├─ utils/
│  │  ├─ category.ts           # 分类路径/查找工具
│  │  └─ unsaved.ts            # 未保存修改标记（管理页离开拦截）
│  ├─ views/
│  │  ├─ HomeView.vue          # 首页（公开）
│  │  ├─ CategoryView.vue      # 分类/子分类题目列表（无限滚动）
│  │  ├─ ArticleDetailView.vue # 题目详情
│  │  ├─ LoginView.vue         # 登录（邮箱/手机号，邮箱支持密码与验证码）
│  │  ├─ RegisterView.vue      # 注册（邮箱/手机号）
│  │  ├─ ForgotPasswordView.vue# 找回密码
│  │  ├─ ProfileView.vue       # 个人信息（含修改昵称）
│  │  ├─ ChangePasswordView.vue# 修改密码
│  │  ├─ AdminView.vue         # 内容管理（添加/编辑 + 分类管理入口）
│  │  └─ NotFoundView.vue      # 404
│  ├─ types/                   # TS 类型定义
│  ├─ styles/theme.css         # 淡黄色主题 + 布局变量
│  ├─ App.vue
│  └─ main.ts
├─ .env.development / .env.production
├─ vite.config.ts              # /api 代理 → http://localhost:8080
└─ package.json
```

---

## 3. 路由与权限守卫

| 路径 | 页面 | 访问控制 |
|---|---|---|
| `/` | HomeView 首页 | 公开 |
| `/category/:slug` | CategoryView 分类/子分类列表 | 需登录 |
| `/article/:slug` | ArticleDetailView 题目详情 | 需登录 |
| `/login` | LoginView 登录 | 仅未登录可访问 |
| `/register` | RegisterView 注册 | 仅未登录可访问 |
| `/forgot-password` | ForgotPasswordView 找回密码 | 仅未登录可访问 |
| `/profile` | ProfileView 个人信息 | 需登录 |
| `/profile/password` | ChangePasswordView 修改密码 | 需登录 |
| `/admin` | AdminView 添加内容 | 需登录 + ADMIN |
| `/admin/edit/:slug` | AdminView 编辑内容 | 需登录 + ADMIN |
| `/admin/dashboard` | 管理后台 · 数据统计 | 需登录 + ADMIN |
| `/admin/articles` | 管理后台 · 题目管理 | 需登录 + ADMIN |
| `/admin/categories` | 管理后台 · 分类管理 | 需登录 + ADMIN |
| `/admin/users` | 管理后台 · 用户管理 | 需登录 + ADMIN |
| `/admin/tags` | 管理后台 · 标签管理 | 需登录 + ADMIN |
| `/admin/logs` | 管理后台 · 操作日志 | 需登录 + ADMIN |
| `/:pathMatch(.*)*` | NotFoundView 404 | 公开 |

守卫逻辑：未登录访问受保护页 → `/login?redirect=原路径`（登录后回跳）；非 ADMIN 访问管理页 → 回首页；已登录访问登录/注册/找回 → 回首页；离开管理编辑页且有未保存修改时弹确认。

---

## 4. 页面设计

### 4.1 首页（HomeView，公开）

顶栏 + 左侧分类 + 欢迎区 + 分类入口卡片；未登录点击分类卡片跳登录，已登录进入分类页。

### 4.2 分类 / 子分类列表页（CategoryView，需登录）

- 左侧栏：当前分类名 + 题目总数；有子分类（如 MySQL）按子分类分组、默认折叠；无子分类直接平铺。
- 内容区：层级面包屑（分类项可点击）、难度筛选、**无限滚动**（每页 10 条，`hasMore` 控制）。
- 子分类页（`/category/mysql-index`）：只列该子分类题目，面包屑保留上级路径。

### 4.3 题目详情页（ArticleDetailView，需登录）

- 左侧 TOC（滚动高亮）+ 中间正文 + 右上管理员“编辑”按钮。
- 正文为后端渲染消毒后的 `contentHtml`，前端 `v-html` + highlight.js 高亮。
- 若题目配置了“文档链接”（如 Arthas 官方文档），标题栏展示“文档链接”按钮，点击在新标签页打开；正文中的站外 http/https 链接同样新标签页打开。
- 面包屑分类项全部可点击；底部上一篇/下一篇。

### 4.4 登录页（LoginView）

- 两个 Tab：**邮箱登录** / **手机号登录**。
- 邮箱登录内再分两种方式（小按钮切换）：**密码登录** / **验证码登录**（验证码输入 + 发送按钮 + 60s 倒计时）。
- 手机号登录：手机号 + 密码。
- 登录失败分别提示“账号不存在”/“密码错误”；底部有“忘记密码？”入口和测试账号提示。

### 4.5 注册页（RegisterView）

- 两个 Tab：**邮箱注册** / **手机号注册**。
- 字段：邮箱或手机号 + 昵称（可选，默认登录账号）+ 密码 + 确认密码。
- 注册成功 → 跳转登录页并自动选中对应 Tab、回填账号（`/login?type=email|phone&account=xxx`）。

### 4.6 找回密码（ForgotPasswordView）

邮箱 + 验证码（60s 倒计时）+ 新密码两次确认 → 重置成功后跳登录页。

### 4.7 个人中心与修改密码

- 个人信息：昵称、邮箱、手机号、注册时间；支持**修改昵称**（弹窗）与修改密码入口，页面居中展示。
- 修改密码：旧密码 + 新密码 + 确认，成功后清登录态跳登录页。

### 4.8 内容管理页（AdminView，仅管理员）

- `/admin` 添加、`/admin/edit/:slug` 编辑（回填原始 Markdown）。
- 表单：标题、slug、摘要、文档链接（选填）、所属分类（叶子分类只显示一次，有子集的保留分组）、难度、标签、正文（Markdown 编辑器 + 预览弹窗）。
- 正文工具栏右侧“导入 Markdown”：选择一个 .md 文件，自动去掉 frontmatter 后填入正文输入框（只负责正文，分类/难度/标签仍由表单填写），导入后可点“预览”查看效果。
- 分类下拉旁有“管理分类”按钮 → 弹窗支持：一级主题行“＋ 子集”、底部“＋ 新主题”、编辑、删除。
- 保存成功后跳详情页；未保存修改离开时弹确认。

---

## 5. 组件设计

| 组件 | 职责 |
|---|---|
| AppHeader | 顶栏：Logo、居中导航、登录按钮或用户下拉（个人中心/修改密码/添加内容/退出） |
| CategorySidebar | 左侧栏：分类树/子分类折叠/题目列表 |
| ArticleCard / DifficultyTag / TocPanel / PrevNextNav | 列表卡片、难度徽标、目录、上一篇/下一篇 |
| VerifyCodeButton | 验证码发送按钮：60 秒倒计时、防重复点击、按 scene（login/reset）调后端 |
| CategoryManageDialog | 分类管理弹窗：新增子集/新主题、编辑、删除 |
| EmptyState | 空数据占位 |

---

## 6. 状态管理（Pinia）

### auth store

```text
state:  token（localStorage）、userInfo（含 role、email、phone）
getters: isLoggedIn
actions:
  login(account, password)      → POST /api/auth/login
  loginByCode(email, code)      → POST /api/auth/login/code
  register(form)                → POST /api/auth/register
  logout()                      → POST /api/auth/logout → 清本地
  fetchProfile()                → GET /api/user/profile
  clearAuth()                   → 清 token/userInfo
```

### category store

```text
state: tree、loaded
actions: fetchTree(force)       → GET /api/categories/tree（缓存）
```

---

## 7. API 层设计

### request.ts

```text
baseURL: /api
请求拦截器：存在 token 时设置 Authorization: <token>
响应拦截器：
  HTTP 401 → 清登录态 → 跳 /login（并 reject，避免误报成功）
  code !== 200 → ElMessage 提示后端 message
```

### 接口模块

| 模块 | 方法 | 说明 |
|---|---|---|
| auth.ts | `login` / `loginByCode` / `sendEmailCode` / `register` / `resetPasswordByCode` / `logout` / `getProfile` / `updateNickname` / `changePassword` | 认证与个人 |
| category.ts | `getCategoryTree` / `createCategoryApi` / `updateCategoryApi` / `deleteCategoryApi` | 分类树 + 管理 |
| article.ts | `getArticles` / `getArticleDetail` / `recordViewApi` / `createArticleApi` / `updateArticleApi` | 内容 |

---

## 8. 关键交互细节

- **验证码按钮**：点击后 60 秒倒计时禁用；邮箱格式非法时按钮禁用。
- **登录回跳**：游客访问受保护页 → 登录成功后回跳原路径。
- **注册回跳**：注册成功按类型跳登录页并预填账号。
- **无限滚动**：触底且 `hasMore` 时加载下一页，切换分类/筛选重置。
- **未保存提醒**：管理编辑页内容有改动时，离开前弹确认（`utils/unsaved.ts`）。
- **分类管理弹窗**：操作成功后刷新分类树，下拉框即时同步；删除有子分类/题目的分类由后端拒绝。

---

## 9. 样式与布局

- 主题：背景 `#fffaf0`、卡片 `#fffdf6`、主色琥珀 `#d9a716`，Element Plus 主色覆盖为琥珀色系。
- 登录/注册/找回密码：居中卡片（380px），与个人中心、修改密码（居中卡片）风格统一。
- 管理页：内容区水平居中，最大宽度 960px。
- 详情页三栏：左侧 TOC | 正文 | 右侧留白/相关（由 CSS 变量控制布局）。

---

## 10. 环境与构建

```text
npm run dev      # 本地开发（http://localhost:5173，/api 代理到 8080）
npm run build    # 产物 dist/，Nginx 托管
```

无 mock：所有数据来自真实后端，启动前需保证后端 8080 + MySQL + Redis 就绪。
