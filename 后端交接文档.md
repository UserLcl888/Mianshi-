# 后端交接文档（前端已完成，待后端按此契约实现）

> 本文档是**前端 → 后端**的交接说明：前端 `user-web/` 已按约定实现并构建通过。后端按本文档的接口契约、数据模型与认证约定实现后，把 `VITE_USE_MOCK` 关掉即可联调。

---

## 一、前端现状

| 项目 | 说明 |
|---|---|
| 代码位置 | `user-web/`（项目根目录下） |
| 技术栈 | Vue 3 + TypeScript + Vite + Vue Router + Pinia + Element Plus + Axios + highlight.js |
| 运行 | `cd user-web && npm install && npm run dev`，访问 http://localhost:5173 |
| 构建 | `npm run build`（已通过验证） |
| 数据来源 | 开发环境默认 `VITE_USE_MOCK=true`（走 `src/mock/`）；联调改 `false` |
| 代理 | Vite 开发代理 `/api` → `http://localhost:8080` |

### Mock 数据与账号

- **账号**：`admin / 123456`（ADMIN，可添加/编辑内容）、`demo / 123456`（USER，只读）；注册账号也可登录（mock 内存中，刷新重置）。
- **内容**：6 个顶级分类（计算机网络/操作系统/MySQL/Redis/Java/系统设计），共 40 道题；MySQL 含 6 个子分类（索引/事务/日志/查询优化/锁/架构与高可用）共 30 题，其余分类各 2 题。
- **访问模型**：未登录只能访问首页；分类/题目接口前端已要求登录；管理页面仅 ADMIN 可进。
- **管理功能（当前为前端 mock）**：管理员可添加/编辑题目，数据存内存，刷新重置。后端接入后改为调用真实管理接口。

---

## 二、联调全局约定

### 2.1 请求地址与请求头

```text
Base URL：/api
登录后请求头：Authorization: <token>
```

```yaml
sa-token:
  token-name: Authorization
```

### 2.2 统一响应格式

```json
{ "code": 200, "message": "ok", "data": {} }
```

分页结构：

```json
{
  "list": [],
  "page": 1,
  "size": 10,
  "total": 40,
  "hasMore": false
}
```

> `hasMore` 供无限滚动判断，**必须返回**，`hasMore = page * size < total`。

### 2.3 错误码与 HTTP 状态

| code | HTTP | 含义 | 前端行为 |
|---|---:|---|---|
| 200 | 200 | 成功 | 取 `data` |
| 40000 | 400 | 参数错误 | 提示 message |
| 40100 | **401** | 未登录 / token 失效 | 清登录态 → 跳 `/login` |
| 40300 | 403 | 无权限 | 提示 message |
| 40400 | 404 | 资源不存在 | 提示 message |
| 40900 | 409 | 冲突（如用户名已存在） | 提示 message |
| 50000 | 500 | 服务器错误 | 提示"网络错误" |

> ⚠️ 401 必须同时返回 HTTP 401，前端 axios 拦截器依赖 HTTP 状态码识别登录失效。

---

## 三、接口契约（前端已实现的调用）

### 3.1 认证接口（公开）

#### POST `/api/auth/register`

```json
{ "username": "zhangsan", "password": "123456", "nickname": "张三" }
// data: null；用户名重复返回 40900
```

#### POST `/api/auth/login`

```json
// 请求
{ "username": "admin", "password": "123456" }
// 响应 data
{
  "token": "xxxxx",
  "userInfo": {
    "id": 0, "username": "admin", "nickname": "管理员",
    "email": "admin@example.com", "role": "ADMIN", "createdAt": "2026-01-01 00:00:00"
  }
}
```

密码错误/账号禁用返回 40000/40100；`role` 必须返回（前端判断管理员）。

### 3.2 内容接口（**全部需登录**，`@SaCheckLogin`）

#### GET `/api/categories/tree`

```json
[
  {
    "id": 3, "name": "MySQL", "slug": "mysql", "parentId": 0,
    "sortOrder": 3, "description": "索引、事务、MVCC、锁、日志",
    "children": [
      { "id": 31, "name": "索引", "slug": "mysql-index", "parentId": 3, "sortOrder": 1, "description": "", "children": [] }
    ]
  }
]
```

#### GET `/api/articles`

```text
Query：categoryId（可选）、difficulty（可选）、page（默认1）、size（默认10）
```

```json
{
  "list": [
    {
      "id": 13, "slug": "mysql-13", "title": "redo log 和 binlog 的区别？",
      "summary": "物理日志 vs 逻辑日志，作用与写入时机。",
      "categoryId": 33, "difficulty": "困难", "tags": ["日志"],
      "viewCount": 3394, "updatedAt": "2026-07-20 10:00:00"
    }
  ],
  "page": 1, "size": 10, "total": 2, "hasMore": false
}
```

⚠️ **分类聚合**：`categoryId=3`（MySQL 顶级）必须返回其全部子分类（31~36）的题目；`categoryId=33`（日志）只返回该子分类题目。返回的 `categoryId` 必须是题目真实所属分类（前端侧边栏按它分组）。

#### GET `/api/articles/{slug}`

```json
{
  "article": {
    "id": 13, "slug": "mysql-13", "title": "redo log 和 binlog 的区别？",
    "summary": "...", "categoryId": 33,
    "categoryName": "日志", "categorySlug": "mysql-log",
    "difficulty": "困难", "tags": ["日志"],
    "contentHtml": "<h2 id=\"overview\">一、概述</h2><p>...</p><pre><code class=\"language-java\">...</code></pre>",
    "toc": [ { "id": "overview", "text": "一、概述", "level": 2 } ],
    "viewCount": 3395, "updatedAt": "2026-07-20 10:00:00"
  },
  "related": [],
  "prev": { "id": 12, "slug": "mysql-12", "title": "Buffer Pool 的作用是什么？" },
  "next": null
}
```

要点：
- `contentHtml` 为已消毒完整 HTML，前端直接 `v-html`；代码块 `<pre><code class="language-xxx">` 供 highlight.js 高亮。
- `toc` 由后端提取，id 与正文锚点一致。
- **`categorySlug` 必须返回**：子分类题目返回子分类 slug（如 `mysql-log`），前端据此生成面包屑（首页/MySQL/日志）与侧边栏。
- 浏览量在请求详情时 +1。
- 题目不存在返回 40400；`prev/next` 为 null 表示无上一篇/下一篇。

### 3.3 登录用户接口

#### POST `/api/auth/logout`

#### GET `/api/user/profile`

```json
// 响应 data：UserInfo（同 login 返回结构）
```

#### PUT `/api/user/password`

```json
{ "oldPassword": "123456", "newPassword": "654321" }
// 旧密码错误返回 40000；前端改密成功后清登录态跳登录页
```

### 3.4 管理接口（`/api/admin/**`，仅 ADMIN）

前端管理页当前调用前端 mock（内存），后端接入后改为调用以下接口：

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/admin/articles` | 新增题目 `{title, summary, categoryId, difficulty, tags[], contentMd}` → 返回新题（含 slug） |
| PUT | `/api/admin/articles/{id}` | 编辑题目（同上 payload，保留 slug） |
| GET | `/api/articles/{slug}` | 编辑页预填用（详情接口） |

---

## 四、数据模型要求

| 表 | 前端强依赖字段 |
|---|---|
| `category` | id、name、slug、parentId、sortOrder、description、children（组装，支持多级） |
| `article` | id、slug、title、summary、categoryId、difficulty、tags、status、contentMd、contentHtml、viewCount、updatedAt |
| `article_tag` / `tag` | 返回给前端的 `tags` 为字符串数组 |
| `user` | id、username、nickname、email、role、status、createdAt |

种子账号：`admin/123456`（ADMIN）、`demo/123456`（USER），密码 BCrypt 加密。

---

## 五、Sa-Token 认证约定

```text
登录：StpUtil.login(userId) → 返回 token
内容/用户接口：@SaCheckLogin（未登录返回 HTTP 401 + code 40100）
管理接口：@SaCheckRole("ADMIN")（非管理员返回 HTTP 403 + code 40300）
登出：StpUtil.logout()
禁用用户：StpUtil.kickout(userId)
```

---

## 六、后端开发待办清单

### M1：内容查询（联调前提，先做）

- [ ] 建表：user / category / article / tag / article_tag / admin_log
- [ ] 种子数据：admin、demo 账号；6 顶级分类 + MySQL 6 子分类；每分类若干题（与 mock 一致便于对照）
- [ ] `GET /api/categories/tree`（需登录，含多级树）
- [ ] `GET /api/articles`（需登录，**顶级分类递归聚合子分类**、分页、hasMore）
- [ ] `GET /api/articles/{slug}`（需登录，详情 + toc + categorySlug + related + prev/next + 浏览量）

### M2：认证与用户

- [ ] Sa-Token（token-name=Authorization、Redis 会话存储）
- [ ] `POST /api/auth/register`、`POST /api/auth/login`、`POST /api/auth/logout`
- [ ] `GET /api/user/profile`、`PUT /api/user/password`
- [ ] 异常映射（401/403）、登录限流

### M3：管理接口（供管理端联调）

- [ ] `POST /api/admin/articles`、`PUT /api/admin/articles/{id}`（新增/编辑题目，渲染 HTML + 清缓存）
- [ ] `/api/admin/users` 系列
- [ ] `/api/admin/categories`、`/api/admin/tags` CRUD、批量导入
- [ ] `/api/admin/stats/*`、`/api/admin/logs`

### 联调步骤

```text
1. 后端启动在 8080（MySQL + Redis 就绪，执行 init.sql）
2. 前端 user-web/.env.development 中 VITE_USE_MOCK=false
3. npm run dev → http://localhost:5173
4. 验证：游客访问首页 → 登录（demo/123456）→ 分类/子分类 → 详情 → 个人中心
5. admin/123456 验证：详情页编辑按钮 → 编辑保存 → 新增内容
6. npm run build 出生产包交给 Nginx
```

---

## 七、注意事项

- **内容接口全部需登录**：未登录访问返回 HTTP 401（前端会跳登录）。
- **hasMore 必须准确**：`hasMore = page * size < total`。
- **顶级分类聚合子分类**：列表接口必须递归包含子分类题目。
- **categorySlug 必须返回**：详情接口缺它会破坏面包屑和侧边栏。
- **content_html 必须消毒**：前端直接 `v-html`，XSS 防线在后端。
- **401 必须返回 HTTP 401**，否则前端无法识别登录失效。
