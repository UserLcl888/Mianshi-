# 后端交接文档（前后端已实现并联调完成）

> 本文档说明当前系统的**实际接口契约、数据模型与认证约定**，供后续开发/维护参考。前端 `user-web/` 与后端 `interview-backend/` 均已实现，直接以真实 API 联调（无 mock）。

---

## 一、项目现状

| 项目 | 说明 |
|---|---|
| 前端 | `user-web/`：Vue 3 + TypeScript + Vite + Element Plus |
| 后端 | `interview-backend/`：Spring Boot 3 + MyBatis-Plus + Sa-Token + Redis + MySQL 8 |
| 数据库 | MySQL `interview` 库（MySQL 8，utf8mb4） |
| 缓存/会话 | Redis 127.0.0.1:6379（密码 java），Sa-Token 会话 + 业务缓存 |
| 运行 | 后端 8080；前端 `npm run dev` 5173，`/api` 代理到 8080 |

### 种子账号（密码均为 `123456`）

| 登录标识 | 角色 | 说明 |
|---|---|---|
| `2090323327@qq.com` | ADMIN | 管理员，可添加/编辑内容、管理分类 |
| `2090323328@qq.com` | USER | 普通用户，只读浏览 |

> 登录标识为邮箱或手机号；`username` 为内部字段（admin/demo），登录不再使用。

---

## 二、联调全局约定

### 2.1 请求地址与请求头

```text
Base URL：/api
登录后请求头：Authorization: <token>
```

### 2.2 统一响应格式

```json
{ "code": 200, "message": "ok", "data": {} }
```

分页结构（列表接口）：

```json
{ "list": [], "page": 1, "size": 10, "total": 40, "hasMore": false }
```

### 2.3 错误码与 HTTP 状态

| code | HTTP | 含义 | 前端行为 |
|---|---:|---|---|
| 200 | 200 | 成功 | 取 `data` |
| 40000 | 400 | 参数/业务错误 | 提示 message |
| 40100 | **401** | 未登录 / token 失效 | 清登录态 → 跳 `/login` |
| 40300 | 403 | 无权限 | 提示 message |
| 40400 | 404 | 资源不存在 | 提示 message |
| 40900 | 409 | 冲突（邮箱/手机号已注册等） | 提示 message |
| 50000 | 500 | 服务器错误 | 提示"网络错误" |

> ⚠️ 401 必须同时返回 HTTP 401，前端 axios 拦截器依赖 HTTP 状态码识别登录失效。

---

## 三、接口契约

### 3.1 公开接口（无需登录）

#### POST `/api/auth/register`

```json
// 邮箱注册
{ "email": "test@qq.com", "password": "123456", "nickname": "测试" }
// 手机号注册
{ "phone": "13800138000", "password": "123456" }
// username 可选；昵称不填默认登录账号（邮箱@前部分/手机号）；邮箱或手机号至少一项
```

重复注册返回 40900；邮箱/手机号格式错误返回 40000。

#### POST `/api/auth/login`

```json
{ "account": "2090323327@qq.com", "password": "123456" }
// 响应 data
{
  "token": "xxxxx",
  "userInfo": {
    "id": 1, "username": "admin", "nickname": "管理员",
    "email": "2090323327@qq.com", "phone": null, "role": "ADMIN",
    "createdAt": "2026-01-01 00:00:00"
  }
}
```

- `account` 同时匹配邮箱或手机号；账号不存在返回 `40000 账号不存在`，密码错误返回 `40000 密码错误`。

#### POST `/api/auth/code/email`

```json
{ "email": "2090323327@qq.com", "scene": "login" }
// scene：login（验证码登录）| reset（找回密码）；邮箱必须已注册
// mock 模式（app.code.mock=true）响应 data 含 debugCode，真实模式 data 为空
```

#### POST `/api/auth/login/code`

```json
{ "email": "2090323327@qq.com", "code": "123456" }
// 响应 data 同 /login；未注册邮箱返回 40000
```

#### POST `/api/auth/reset-password`

```json
{ "email": "2090323327@qq.com", "code": "123456", "newPassword": "654321" }
```

成功后旧会话被踢下线。

#### GET `/api/categories/tree`

```json
[
  {
    "id": 8, "name": "Java集合", "slug": "java-collection", "parentId": 0,
    "sortOrder": 2, "description": "", "children": []
  }
]
```

### 3.2 登录用户接口

#### GET `/api/articles`

```text
Query：categoryId（可选）、difficulty（可选）、page（默认1）、size（默认10）
```

- 顶级分类 `categoryId` 递归聚合子分类题目；`hasMore = page * size < total`。

#### GET `/api/articles/{slug}`

```json
{
  "article": {
    "id": 37, "slug": "java-1", "title": "HashMap 的底层实现原理？",
    "summary": "...", "categoryId": 8,
    "categoryName": "Java集合", "categorySlug": "java-collection",
    "difficulty": "MEDIUM", "tags": ["HashMap", "集合"],
    "contentMd": "## 3.1 底层原理\n...",
    "contentHtml": "<h2 id=\"3-1-底层原理\">...</h2><pre><code class=\"language-java\">...</code></pre>",
    "toc": [ { "id": "3-1-底层原理", "text": "3.1 底层原理", "level": 2 } ],
    "viewCount": 100, "updatedAt": "2026-08-10 17:23:56"
  },
  "prev": null,
  "next": { "id": 38, "slug": "java-2", "title": "..." }
}
```

要点：`contentMd`（编辑页回填用）、`contentHtml`（已消毒，直接 `v-html`）、`toc`（锚点 id 与正文一致）、`categorySlug`（面包屑/侧边栏依赖）。

#### POST `/api/articles/{id}/view`

浏览上报：同用户 24 小时内只计一次，返回最新 `viewCount`。

#### POST `/api/auth/logout`

#### GET `/api/user/profile`

返回 UserInfo（同 login 结构）。

#### PUT `/api/user/profile`

```json
{ "nickname": "新昵称" }
```

返回最新 UserInfo。

#### PUT `/api/user/password`

```json
{ "oldPassword": "123456", "newPassword": "654321" }
```

旧密码错误返回 40000；成功后被踢下线重新登录。

### 3.3 管理接口（`/api/admin/**`，仅 ADMIN）

#### 题目

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/admin/articles` | 管理端题目列表（含草稿，`?categoryId=&difficulty=&page=&size=`） |
| POST | `/api/admin/articles` | 新增 `{title, slug?, summary, docUrl?, categoryId, difficulty, tags[], contentMd}` → 返回新题（含 slug） |
| PUT | `/api/admin/articles/{id}` | 编辑（payload 同上，保留原 slug） |
| DELETE | `/api/admin/articles/{id}` | 删除 |

#### 分类

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/admin/categories` | 新增 `{name, slug, parentId, sortOrder, description}` |
| PUT | `/api/admin/categories/{id}` | 编辑名称/slug/排序/描述 |
| DELETE | `/api/admin/categories/{id}` | 删除（有子分类或题目时返回 40000） |

#### 用户 / 标签 / 统计 / 日志

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/admin/users` | 用户列表 `?keyword=&status=&page=&size=` |
| GET/POST | `/api/admin/users[/{id}]` | 详情 / 新增 |
| PUT | `/api/admin/users/{id}` | 编辑昵称/邮箱/角色 |
| PUT | `/api/admin/users/{id}/status` | 禁用/启用 |
| PUT | `/api/admin/users/{id}/password` | 重置密码 |
| DELETE | `/api/admin/users/{id}` | 删除用户 |
| GET/POST/PUT/DELETE | `/api/admin/tags[/{id}]` | 标签管理 |
| GET | `/api/admin/stats/overview` | 用户数/题目数/分类数/发布数 |
| GET | `/api/admin/stats/top-articles` | 浏览量 Top10 |
| GET | `/api/admin/stats/category-stats` | 各顶级分类（聚合子分类）的 `{id, name, viewCount, articleCount}` |
| GET | `/api/admin/logs` | 操作日志分页 |

---

## 四、数据模型要点

| 表 | 要点 |
|---|---|
| `user` | `username`（唯一，内部标识）、`password_hash`、`rootpassword`（明文演示）、`nickname`、`email`、`phone`（可空唯一，未填存 NULL）、`role`、`status` |
| `category` | `name/slug/parentId/sortOrder/description`，多级 |
| `article` | `slug` 唯一、`difficulty` EASY/MEDIUM/HARD、`content_md` + `content_html`、`view_count` |
| `tag` / `article_tag` | 标签与题目多对多，接口返回 `tags: string[]` |
| `admin_log` | 管理操作日志（admin_id/action/target_type/target_id/detail） |

---

## 五、Sa-Token 认证约定

```text
登录：StpUtil.login(userId) → 返回 token
登录用户接口：拦截器校验（未登录返回 HTTP 401 + code 40100）
管理接口：@SaCheckRole("ADMIN")（非管理员返回 HTTP 403 + code 40300）
登出：StpUtil.logout()
禁用用户/找回密码：StpUtil.kickout(userId)
```

白名单（公开）：`/api/auth/login`、`/api/auth/register`、`/api/auth/code/email`、`/api/auth/login/code`、`/api/auth/reset-password`、`/api/categories/tree`。

---

## 六、邮件与验证码配置

```yaml
app:
  code:
    ttl-minutes: 5      # 验证码有效期
    max-tries: 5        # 错误次数上限
    resend-seconds: 60  # 重发冷却
    daily-limit: 10     # 单邮箱每日上限
    mock: false         # true=打印日志并返回 debugCode；false=真实 SMTP 发信
spring:
  mail:
    host: smtp.qq.com
    port: 465
    username: <发件邮箱>
    password: <SMTP 授权码>
    properties:
      mail.smtp.ssl.enable: true
```

---

## 七、联调/部署步骤

```text
1. 启动 MySQL（interview 库）与 Redis（6379）
2. 启动后端：mvn spring-boot:run（8080，需 JDK17+）
3. 启动前端：cd user-web && npm install && npm run dev（5173）
4. 验证：游客首页 → 注册/登录（邮箱或手机号）→ 分类/详情 → 个人中心（改昵称/改密码）→ 找回密码
5. admin（2090323327@qq.com/123456）：详情页编辑 → 添加内容 → 分类管理
6. npm run build 出生产包交给 Nginx
```
